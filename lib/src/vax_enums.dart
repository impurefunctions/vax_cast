enum FHIR_V {
  r4,
  stu3,
  dstu2,
}

enum Gender {
  female,
  male,
  unknown,
}

enum TargetStatus {
  not_satisfied,
  satisfied,
  skipped,
}

enum EvalStatus {
  extraneous,
  not_valid,
  sub_standard,
  valid,
}
